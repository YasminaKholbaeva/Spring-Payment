spring:
  application:
    name: payment

  server:
    port: 8081

  datasource:
    url: jdbc:postgresql://localhost:5432/payment_db
    username: postgres
    password: Yasmina060701#
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  jackson:
     serialization:
        indent_output: true  # ??????? ??????????? JSON

  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.type.descriptor.sql.BasicBinder: TRACE